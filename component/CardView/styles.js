import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    container: {
        padding: 6,
        margin: 11,
        borderBottomWidth: 2,
        borderColor: 'black'
    },
    textView: {
        fontSize: 20,
        fontWeight: 'bold'
    }
})

export default styles